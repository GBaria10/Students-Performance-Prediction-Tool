import { describe, it, expect } from "vitest";

describe("LoginPage basics", () => {
  it("passes sample check 1", () => expect(true).toBe(true));
  it("passes sample check 2", () => expect(2 + 2).toBe(4));
  it("passes sample check 3", () => expect([1, 2, 3]).toContain(2));
});
